name: First Cron Job

on: [push]

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, ${{ secrets.WORLD }}
        env:
          CHECK_ENV: halo-halo
